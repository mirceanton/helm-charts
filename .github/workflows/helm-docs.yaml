---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Helm Docs
permissions: { contents: write }

on:
  workflow_dispatch:
    inputs:
      dry-run:
        description: Dry Run
        default: true
        required: false
        type: boolean
  push:
    paths:
      - "charts/**/Chart.yaml"
      - "charts/**/values.yaml"
      - "charts/**/README.md.gotmpl"

jobs:
  discover-charts:
    runs-on: ubuntu-latest
    outputs:
      charts: ${{ steps.set-charts.outputs.charts }}
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Find Helm Charts
        id: set-charts
        run: |
          CHARTS=$(find charts -type f -name "Chart.yaml" | xargs dirname | xargs -n1 basename | jq -R -s -c 'split("\n")[:-1]')
          echo "charts=$CHARTS" >> "$GITHUB_OUTPUT"
          echo "Found charts: $CHARTS"

  generate-docs:
    needs: [discover-charts]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        chart: ${{ fromJson(needs.discover-charts.outputs.charts) }}
    steps:
      - name: Generate Token
        uses: actions/create-github-app-token@df432ceedc7162793a195dd1713ff69aefc7379e # v2.0.6
        id: app-token
        with:
          app-id: "${{ secrets.BOT_APP_ID }}"
          private-key: "${{ secrets.BOT_APP_PRIVATE_KEY }}"

      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          token: "${{ steps.app-token.outputs.token }}"

      - name: Get Changed Files
        id: changed-files
        uses: bjw-s-labs/action-changed-files@b1144fc772fca235a50902c7bb6cc431cc7d8e27 # v0.3.2
        with:
          patterns: |
            ./charts/${{ matrix.chart }}/Chart.yaml
            ./charts/${{ matrix.chart }}/values.yaml
            ./charts/${{ matrix.chart }}/README.md.gotmpl

      - name: Setup mise
        if: steps.changed-files.outputs.changed_files != '[]' || github.event_name == 'workflow_dispatch'
        uses: jdx/mise-action@7a111ead46986ccad89a74ad013ba2a7c08c9e67 # v2.2.1

      - name: Generate helm docs
        if: steps.changed-files.outputs.changed_files != '[]' || github.event_name == 'workflow_dispatch'
        working-directory: ./charts/${{ matrix.chart }}
        run: mise exec -- helm-docs

      - name: Check for changes
        id: git-check
        if: |
          (steps.changed-files.outputs.changed_files != '[]' && github.ref_name == 'main')
          ||
          ( github.event_name == 'workflow_dispatch' && inputs.dry-run == false)
        run: |
          git diff --exit-code ./charts/${{ matrix.chart }}/README.md || echo "changes=true" | tee "$GITHUB_OUTPUT"

      - name: Git config
        if: steps.git-check.outputs.changes == 'true'
        run: |
          git config --global user.name 'mr-borboto[bot]'
          git config --global user.email '170036086+mr-borboto[bot]@users.noreply.github.com'

      - name: Commit and push changes
        if: steps.git-check.outputs.changes == 'true'
        run: |
          git add ./charts/${{ matrix.chart }}/README.md
          git commit -m "docs(chart): update ${{ matrix.chart }} README.md"
          git push

  update-summary:
    needs: generate-docs
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Set Job Status
        run: |
          if [[ "${{ needs.generate-docs.result }}" == "success" ]]; then
            echo "Helm documentation successfully updated!"
          else
            echo "::warning::Some charts failed to update documentation. Check job logs for details."
          fi
